# AGENTS.md

## Git workflow

- Commit each changed file as its own independent commit (one file per `git add` + `git commit`), never batch unrelated files into a single commit.
- Follow the existing conventional-commit style (e.g., `docs:`, `chore:`, `feat:`).
